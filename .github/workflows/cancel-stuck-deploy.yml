name: Cancel superseded GoProxy deploy

on:
  push:
    branches: [main]
    paths: ['.github/workflows/cancel-stuck-deploy.yml']

permissions:
  actions: write
  contents: read

jobs:
  cancel:
    runs-on: ubuntu-latest
    steps:
      - name: Cancel hung verifier
        env:
          GH_TOKEN: ${{ github.token }}
          REPO: ${{ github.repository }}
        run: gh api --method POST "repos/$REPO/actions/runs/33356539303/cancel" || true
